# Configuraciones locales
config.municipio.php

# Backups y compresión
*.bak
*.zip
*.rar
*.tar.gz

# Docker
/mysql_data
/sql_data

# Logs
*.log
error_log

# Ignorar temporales
.DS_Store
Thumbs.db

# Dependencias de Composer: regenerables con `composer install`, y
# tcpdf_old/vendor tiene rutas de tests tan largas que rompen git en
# Windows (Filename too long).
vendor/

# Carpetas de limpieza del repo: duplicados y backups viejos movidos aqui
# (nunca borrados) durante las sesiones de orden. Son solo respaldo local,
# no deben terminar en el historial de git.
_archivo_obsoleto_*/
# Anexos subidos por los contribuyentes (RUT, camara de comercio, cedula...).
# En Plesk la carpeta cae dentro del checkout, un nivel arriba de erpsoftsas/.
# Son documentos de personas: no van al repositorio.
anexos_establecimientos/
App/Firma_digital/anexos_establecimientos/
